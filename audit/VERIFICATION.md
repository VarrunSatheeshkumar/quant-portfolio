# Presentation verification — 2026-09-14

Scope: the presentation edits from baseline `b8c41b8`, recorded in housekeeping commit `813bddd` and readability commit `370936c`, plus the correction-record backlinks added during verification. This is a document check, not another statistical audit.

Claude Fable was asked to verify independently but stopped with: “You've hit your session limit · resets 3:50am (Europe/London)”. It returned no completed findings. The owner then instructed Codex to finish; the checks below are Codex's, not an independent Claude certification.

## Meaning and correction records

The four research documents were compared with their preceding versions and the relevant permitted wording in the unchanged [claim ledger](CLAIM_LEDGER.md). No changed research meaning was identified in the final edits. Unique measurements formerly in Established fields remain in the measurement prose; the fields now refer there. Model assumptions, observation units, inspection history, stopping-rule departures and unresolved quantities remain stated.

The [correction record](CORRECTIONS.md) preserves both original correction subsections verbatim. Backlinks identify the original referents of words such as “above”. Its counts are **14 explicitly withdrawn quoted assertions in 12 BTC bullets** and **10 in nine futures bullets**. These are not counts of every correction, or counts of grammatical sentences; replaced and qualified assertions are excluded. The highlighted examples are editorial selections.

The requested replacement personal introduction was applied. The additional proposed research paragraph was omitted under the owner's meaning-preservation rule: describing both programmes as tests of documented effects surviving retail costs, and replacing the settled “neither establishes a tradable strategy” wording, would recast the existing account.

## Document tests

The existing document-test modules pass without modification:

- BTC: **52 quoted phrases**, **5 available data-derived phrases**, zero failures; two historical figures remain identified as uncheckable here.
- Futures: **49 quoted phrases**, zero failures; seven figures remain identified as uncheckable here.

The counts match the baseline. These tests check selected literal phrases, not the validity of every interpretation. Research code, stored results, tests and ledger permitted-wording cells were not changed by this presentation pass.

## Publication checks

All regular working-tree files were included in the signature scan, including ignored and binary files. The Git object/history database was excluded. The scan found personal machine paths in two ignored reproduction logs; those paths were replaced with placeholders, and the affected files were rechecked. No literal IP address, ping URL or common API-key/private-key signature was identified in the final working-tree scan. A signature scan is not a proof that credentials of every possible format are absent, and this is not a Git-history secret audit.

All relative links in all working-tree Markdown files resolve, including the foundation projects, correction record and explicit heading anchors. Neither `.Rhistory` nor `ARCHIVE_NOTES.txt` remains anywhere in the working tree; both names are ignored. The latter was only present in an external review ZIP, not tracked in this repository.

No remote was configured and nothing was pushed. Git cleanliness is checked after recording this verification; the subsequent cold read is recorded separately without changing the portfolio files.
