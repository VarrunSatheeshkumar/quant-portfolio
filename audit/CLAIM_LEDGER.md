# Claim ledger — audit of 2026-09-12, with subsequent corrections

New measurements made in this session are preserved as scripts and outputs under `audit/diagnostics/` (d1–d5, `RUN_DATE.txt`); rows citing them name the file.

This ledger records reviewed assertions from the research READMEs/write-ups, the portfolio's P7/P8 material and the historical remediation records. It contains both superseded assertions and current permitted wording; source locations in the historical assertion column may refer to an earlier file version. Coverage was a spot check and is not an exhaustive certification that every sentence has a row. Later additions must be checked against their own sources; inclusion here is not evidence of independent verification.

Class (a) means a stored or directly computed measurement; (b) an assumption-dependent calculation or interpretation; (c) a documentary/operational statement or an explicitly stated author decision. All classes require source support. Sample counts are measurements. Section I now identifies the type of each later addition, including conditional statements and author choices that were previously grouped incorrectly as operational facts.

Abbreviations: bR/bW/bS = BTC README/WRITEUP/former SUMMARY; fR/fW = futures README/WRITEUP; T = portfolio README; S0/S1 = historical Stage 0/Stage 1 documents; ES = `btc-public/reports/results/event_study.json`; INS/ECO/VM/HAR = the corresponding JSONs in that directory. "Private" denotes the author's unpublished original project repositories; their cited commits and documents are not included. See the [audit guide](README.md) for public/private source boundaries.

## A. Blocker rows

| # | exact assertion (file:line) | source | what the source establishes | assumptions needed | permitted wording |
|---|---|---|---|---|---|
| A1 | "My design estimate for the funding book's standard error was 0.74% a week; the design achieved 0.27 — wrong by nearly threefold" (fW:45); "the standard error the design actually achieved was a third of what was estimated in advance" (fW:55); "under-scored by nearly threefold" (fW:55) | private `reports/CANDIDATES.md:159`: "**MDE:** weekly long–short level, 360 weeks, σ_w ≈ 5%: 0.74 / **1.0% per week**"; `prereg_design.json` (public and private, post-refetch): `se 0.272`, `mde_unadj 0.762`, `mde20 1.050` | The only prior record of 0.74 is labelled MDE, alongside 1.0 as the N = 20 MDE. 0.74/2.80 = 0.264 and 1.0/3.86 = 0.259; 5 %/√360 = 0.264. The achieved SE is 0.272. | That CANDIDATES.md:159 is the "design estimate" the sentence refers to (no other artefact contains 0.74). | withdraw. |
| A2 | "Breadth is the sample size." (fW:45; T:49); "What separates them is breadth: roughly 110 instruments against 27 markets." (fW:55) | As A1; `C18_AUDIT.md` (price leg t 1.83, funding leg t 15.3); `C9.md`/`C1.md` (t 2.12, 2.48) | One cleared statistic (a funding-selection alpha on ~110 perpetuals) and several uncleared statistics (price slopes on 13 commodities). No artefact varies breadth while holding the estimand fixed. | That the difference in t is caused by breadth rather than by estimand, market, period or signal strength. No artefact separates these. | withdraw. |
| A3 | "halt rules: control fails, a candidate clears, or three consecutive failures for one reason." (fW:29) as a description of a procedure that was followed | private `PREREG.md:109`: four registered conditions — "control fails → halt and diagnose; a candidate clears → halt and report; three consecutive failures for the same reason → halt (class dead); data source unavailable → halt"; private `LOG.md` (C9 10:41, C1 10:42, C11 10:42 not cleared; C18 CLEARED 17:41 on 09-11; C15 NOT CLEARED 17:42); `PHASE4_REPORT.md` at `ed42f14` (09-10 10:43:38): "Halt rule triggered: C9, C1 and C11 all failed the same way … C18, C15 and C5 were not run (C5 is in the same class and is predicted to fail identically; C18 and C15 are different samples and remain runnable)"; at `b7cd128` (09-11 17:43:37): "halted on the three-same-reason rule … — then, on instruction, C18 and C15 to complete the scoreable set. C5 was not run" | Four conditions were registered; the WRITEUP names three. The three-same-reason halt was recorded as triggered after C11; the same report recorded C18 and C15 as different samples that remained runnable; the later report records that they were then run "on instruction". C15 was run one minute after C18 was recorded as cleared. | None for the sequence; it is in the log. Whether running C18/C15 was consistent with the registered rule depends on which of the two contemporaneous statements governs; the ledger reports both. | The four registered conditions may be quoted with the record beside them: after C11 the report was titled "halted" and stated that C18 and C15 remained runnable; the next day C18 and C15 were run, the report recording "on instruction"; C15 was run after C18 was recorded as cleared, which the registered clear-halt condition did not provide for. The original private logs and commits are not included; the published write-up and evidence record now report their contents. |
| A4 | (S0 v1, now removed) "C15 is inside the class the rule declared dead" | `PHASE4_REPORT.md` at `ed42f14`: "C18 and C15 are different samples and remain runnable" | At the time of the halt the operator recorded C15 as outside the halted class. | — | withdraw (already removed from S0 v2). |
| A5 | "Per-version pre-commitments (BTC v2–v13)" (S0 v1, removed); S0 v2: "For v2 through v12 … a commit whose message records the hypothesis and criteria before a separate commit recording the result … For v13 there is one commit" | the private btc-project commit log: v2 `a92f5e5` 10:06:20 → `1241700` 10:21:30; v3 `d01ab16` → `8f943a1`; v4 `a8e363a` → `fe845ba`; v5 `6633ef9` → `6d66d4f`; v6 `1836ae1` → `66e13ae`; v7 `0cab80e` → `dca1346`; v8-prep `5147562` → `c7ea11c`; v9 `21a7a31` → `b6c1485`; v10 `50f7632` → `ae9d7a7`; v11 `c33205e` → `fecdafd`; v12 `dce7f8d` → `96d2d03`; v13 single commit `4c8adf8` (VERSIONS.md, stage code, results, tests, DECISIONS together) | Commit order for v2–v12: a commit describing hypothesis/criteria precedes the commit describing the result. For v13 no such order exists. Commit order does not establish execution order for any version. | That commit messages describe their contents (not checked line by line for v3–v12). | v2–v12: "a commit recording the hypothesis precedes the commit recording the result (author-dated, local, unpushed)". v13: "hypothesis, code and result were committed together; commit order is silent on sequence". Nothing may be said about when the v13 criterion was written. |
| A6 | (S1 v1, removed) "framing 1's matched difference and framing 4 have no MDE/CI at all"; S1 v2 as corrected | ES `framings.1_acceleration`: `matched_diff_bps 8.724, n_cells 11` (no interval keys); `placebo {real 8.724, placebo_abs_p95 9.941, clears false, n_placebo 300}`; `abs_move_slope {n 129875, n_days 1452, slope 332.18, ci_lo 197.42, ci_hi 446.35, mde 172.48}`; ES `framings.4_regime`: `weekend {matched_diff_bps −1.37, n_cells 178}`, `asia {1.161, 45}` | Framing 1 has one statistic without an interval (the matched difference) and one with an interval and an MDE (the absolute-move slope). Framing 4 has two point differences without intervals. | — | "Framing 1's matched difference has no interval; its absolute-move slope has an interval [197, 446] and MDE 172. Framing 4's differences have no intervals." |
| A7 | Framing 1 sign discrepancy: public ES `abs_move_slope.slope = +332.18` versus private v2 (VERSIONS.md v2 table) "Absolute move, 60m, dose-response −0.000532, CI [−0.00065, −0.00004] — significant and the wrong sign for acceleration" | Units, public: `event_study.py:60` `cont_* = (logpx[nxt] − logpx[b]) × sign × 1e4` (bps, signed toward the forced-flow direction); `:254-255` `_slope(absm = |cont_60m|, x = "share")` with `share = mass/total` (`:111`, a fraction of total map mass); `_slope` (`:141-173`) is a within-cell (dist_q, vol_q, hour_q) demeaned OLS slope, day-block bootstrapped. So +332 = bps of absolute 60-minute move per unit of map share, positive. Units, private: private btc-project `stages/v2_cluster_event_study.py:136` `absmove = |logpx[nxt] − logpx[idx]|` (decimal); `:214,287` regressor `logshare = log(share)`; output key `slope_per_log_share`. So −0.000532 = decimal absolute move per unit of log share, negative. Registered v2 statistic (VERSIONS.md:105-108): a ratio of mean signed continuations; neither absolute-move regression is the registered statistic. | The public linear-share regression has a positive estimated slope with an interval excluding zero; the private log-share regression has a negative estimated slope with an interval excluding zero. They are different specifications (linear vs log regressor; bps vs decimal outcome; cell definitions and samples not shown to be the same). | Whether the two would agree on a common specification: unknown. | "The public within-cell regression of absolute 60-minute move (bps) on map share has slope +332 [197, 446]; the private v2 regression of absolute move (decimal) on log share had slope −0.00053 [−0.00065, −0.00004]. The specifications differ and do not support a common directional conclusion." Any sentence asserting a direction for absolute movement at mapped levels: withdraw. |
| A8 | "what remains is the funding accrual itself, +0.47% per week" (fW:35); S1 v1 (removed) "the funding leg is mechanical" | `candidates/common.py:356-362`: `sig = fr.loc[t−7d : t−1d].sum()` (trailing funding, as-of); `fund = fr.loc[t+1d : t1].sum()` (funding accrued while held); `tot_long = ret − fund`, `tot_short = −ret + fund`; `C18_AUDIT.md`: funding leg +0.535 %/wk (SE 0.035, t 15.3), 2022+ +0.466 (SE 0.034) | Selection uses funding over the previous 7 days; the book receives funding over the following week. The funding leg's mean is the difference between future funding received on the short quintile and paid on the long quintile, selected on past funding. It is an estimate of funding persistence across adjacent weeks, not an identity. Only the sign convention (longs pay positive funding) is fixed by the contract. | That `fr` (daily-summed Binance funding rates) equals funding actually paid on the held notional at the actual payment timestamps (not reconciled; plan 2.6). | "The funding leg — future funding received on a book selected by trailing funding — is +0.47 %/wk (SE 0.03) after 2021." "Funding accrual itself" and "mechanical": withdraw. |
| A9 | "+0.93% per week net of a 20-basis-point round trip at 41% weekly turnover" (fW:35; fR:15 "+0.93% per week net of cost") | `run.py:221` `cost = turnover.mean() × 0.20`; `common.py:373-378` turnover = 1 − \|union∩prev\|/\|union\| over long ∪ short names; `common.py:370` `ls = r_long + r_short` (each leg an equal-weight mean, so `ls` is per one leg's notional on a 200 %-gross book). Preserved diagnostic `audit/diagnostics/d1_c18_traded_notional.py` (+ `.out`, run 2026-09-12 from the cached book): mean names/leg 22.2; mean Σ\|Δw\| per week = **1.888** in one-leg units (gross Σ\|w\| = 2.0); ≈ 20.8 entries, 20.7 exits, 2.5 flips per week; union turnover 0.4147; charged cost 0.083 %/wk | The charged cost is (share of names replaced) × 20 bp. The sum of absolute target-weight changes each week, under equal weights rebalanced weekly with drift ignored, is 1.888 × one leg's notional (94 % of gross). On the same per-one-leg basis as `ls`, that quantity × per-side cost is 0.094 % at 5 bp, 0.189 % at 10 bp, 0.472 % at 25 bp. "1.9" is Σ\|Δw\| in one-leg units per week; it is not a multiplier on the turnover share (1.9 × 0.415 × 10 bp = 0.079 % applies a per-side cost to a fraction of names, not to weight changes). | Target-weight changes ignoring drift are not realised rebalance trades (drift within the week changes actual weights, so realised traded notional can differ either way); equal weights 1/n per leg; per-side cost constant across names and dates; no financing, margin or funding-timing cost. The per-side cost applicable to Binance USDT-M small-cap perpetuals: unknown here (external). | "The charged cost, 0.083 %/wk, prices a one-leg round trip on the share of names replaced. The sum of absolute target-weight changes is 1.9 × one leg's notional per week (drift ignored); at 10 bp per side that is 0.19 %/wk, at 25 bp 0.47 %/wk. Net alpha with costs entered into the weekly series has not been computed." "+0.93 % net": withdraw. |
| A10 | "Trend alone had life: Sharpe 0.24 after costs, 10.0 basis points gross per side traded against 3.24 of cost, timing +0.44 against a standing tilt of −0.22" (fW:21) | `data/stages/s2_stats.json`: `sharpe_net 0.238, sharpe_gross 0.352, tilt_gross_sharpe 0.222, timing_gross_sharpe 0.130`; `s6_stats.json`: `sharpe_net 0.064, tilt_gross −0.234, timing_gross 0.446`; `backtest.py:113-131` (one fixed-seed plain block permutation; `timing` = gross Sharpe − shuffled gross Sharpe) | 0.24, 10.0, 3.24 are trend's. +0.44 / −0.22 are the combined three-signal book's. For trend: gross Sharpe 0.352; gross Sharpe of one plain block-shuffled path 0.222 (the code's label: `tilt`); difference 0.130 (the code's label: `timing`). | That a difference of two Sharpe ratios from one permutation draw is a meaningful quantity; it is not an additive return attribution. | "Trend: net Sharpe 0.24; gross Sharpe 0.35; gross Sharpe of one plain block-shuffled path 0.22 (the code labels this `tilt`); difference 0.13 (the code labels this `timing`); one permutation draw." The −0.22 / +0.44 figures: withdraw from the trend sentence. "Had life": withdraw. |
| A11 | "Eight structures were costed … The best of them resolves 41.8bps against a 10bps band." (bW:125-128; bS:61-63) | INS: `n_structures_costed 8`; `structures.strip_7d_wide.mde_bps 41.77` (n 242); `instrument.py:364-372` mde = 2.802 × sd_paired/√n with sd_paired = SD(long − short) × √0.5 | Eight structures have MDEs; the smallest is 41.8 bps on the 7-day wide strip. sd_paired for straddles: `run_straddle` is evaluated at side +1 and −1 on the same trade (`:264-270`), so long − short = 2×(payoff − mid − hedge P&L) up to fee terms; SD(long − short)×√0.5 = √2 × SD(one side's pre-spread P&L) approximately. For strips, `run_strip` at ±1 likewise (`:340-347`). | That the intended per-trade dispersion is that of one side of the trade (then the stored value is √2 too large) — or that the intended estimand is the paired difference (then the label "per trade" is wrong). Which was intended: unknown. MDE formula assumes independent trades; entries are weekly Fridays and 30-day legs overlap (`:261-291`). | "Eight structures were costed; the smallest stored MDE is 41.8 bps, computed as 2.80 × SD(long − short)/√(2n) on weekly entries treated as independent." No statement that any structure "resolves" the band. |
| A12 | "An idealised variance swap — zero spread, zero fees, zero hedging error — still has a minimum detectable effect of 81.6bps … reaching 10bps from it needs 67 times the sample: roughly 309 years" (bW:130-134; bS:76-80; T:44) | INS: `best_idealised_mde_bps 81.61` = `idealised_2021_on.strip_7d_wide` (n 190, years 4.64); `idealised.strip_7d_wide.mde_bps 137.27` (n 242, full sample); `sample_multiple_needed 66.6 = (81.6/10)²`; `years_needed 309 = 66.6 × 4.64`; `instrument.py:400-406` comment: 2020 raises the floor, so the "conservative claim is the one made on the calmer sub-sample" | 81.6 is the 2021-onward subsample; the full-sample value is 137.3. The subsample was chosen with knowledge that the full sample gives a higher figure, with the stated rationale that a lower floor is the conservative direction for "cannot resolve". The √2 of A11 applies. 309 years is (MDE/band)² × sample years, assuming 1/√n scaling and stationarity. | Independence of weekly variance P&L; stationarity over the extrapolation; the √2 convention. | "On the 2021-onward subsample (190 weekly strips) the stored idealised MDE is 81.6 bps (137.3 on the full sample); (81.6/10)² = 67; 67 × 4.6 years = 309." Nothing may be said about what "nothing tradeable" can or cannot beat. |
| A13 | "The binding constraint was therefore never the instrument and never really the sample. It is the dispersion of the quantity being measured … no structure reduces the variance of the thing it is measuring … expected edge and minimum detectable effect both scale with dispersion, so the ratio is independent of the structure entirely" (bW:136-142; bS:82-84) | `economics.py:66-80`: edge = ρ × √(2/π) × sd per structure with one ρ range applied to every structure; `instrument.py` MDE = 2.80 × sd/√n; ECO `assumptions[5]` (`economics.py:54`): "The same rho is assumed for every structure, which flatters any structure whose dispersion comes from noise a volatility forecast cannot see." | Under the code's assumptions edge/MDE = ρ√(2/π)√n/2.80, which does not depend on sd. The code assumes a common ρ across structures and records that this assumption may flatter noisy structures; nothing in the artefacts supports the common-ρ assumption. | That ρ is the same for every structure — an assumption stated by the code and unsupported by any measurement. | withdraw. (Permitted as algebra only: "under the code's assumption of a common ρ, edge/MDE does not depend on the structure's dispersion; the assumption is unsupported.") |
| A14 | "worth 45.7 to 67.3bps per trade on the cheapest costed structure … against that structure's measured round trip of 20.1bps … Every assumption in that conversion favours the trade" (bW:116-118; bS:67-70) | ECO: `best_edge 45.7–67.3`, `best_cost 20.05`; `economics.py:71` ρ = √max(gain,0); `instrument.py:229,250`: premium at ask (long) / bid (short); `cost = (fees + hedge_cost)/S0`; ECO `assumptions[2]`: "None of the improvement over HAR is assumed to be in the option price already"; `vol_model.py:28-36` FEATURES includes `log_dvol` | Edge = √ΔR² × √(2/π) × stored sd. Cost excludes the option bid-ask spread (which sits inside `pnl`, not `cost`); strips' cost includes their half-spread crossing. Model A's features include DVOL, an option-implied quantity, while the economics assumes none of the improvement is in option prices. | ρ = √ΔR² (a correlation between a log-vol forecast error and a 7-day variance payoff is assumed equal to the square root of an R² gain at 1h/4h); ρ transfers to hedging noise; the √2 of A11. | "Cost of 20.1 bps comprises fees and hedge fees; the option bid-ask spread is not included." "Every assumption favours the trade": withdraw. The 45.7–67.3 figure: report only as the output of the stated formula with its assumptions listed; no "worth". |
| A15 | "Its MDE is 73.7bps, larger than the most generous edge, so the sample cannot tell +25.7bps from zero" (bS:70-72; bW:119-121) | ECO/INS as above; `structures.straddle_7d_daily_hedge {sd 372.7, mde 73.66, n 201, cost 20.05}`; `straddle_7d_hourly_hedge {sd 236.2, mde 46.68, cost 46.73}` | The daily-hedged straddle has the larger stored dispersion (373 vs 236 bps) and therefore both the larger "edge" (∝ sd) and the larger MDE. | As A11, A13, A14. | "The stored MDE for the daily-hedged straddle is 73.7 bps; the stored edge range is 45.7–67.3." No inference about what "the sample can tell". |
| A16 | "521 of them, about ten years of weekly ones" (bW:143; bS:73) | ECO `independent_trades_needed 521`, `years_needed_weekly 10.0` = (2.802/(ρ_lo × √(2/π)))² with ρ_lo 0.154 | Arithmetic reproduces from the stored ρ and formula. | As A14 (ρ), independence. | Report as arithmetic under the formula's assumptions only. |
| A17 | (Stage 1 v1, removed; audit-forwarded) "the s9 consistency check cannot fail" | `stages/s9.py:50-51`: `se2 = sqrt((1 + 0.5×S_train²)/2)`; `consistent = |S_holdout − S_train| ≤ 2×se2`; with S_train 0.064: se2 0.708, threshold 1.416; \|−1.28 − 0.06\| = 1.34 | The check passes for any hold-out Sharpe in [−1.35, +1.48]; −1.28 is inside. It can fail outside that range. | Lo's iid SE formula applies to the two-year annualised Sharpe. | "The s9 consistency check passes for any hold-out Sharpe within ±1.42 of the training value; the observed −1.28 lies 1.34 below it." "Cannot fail": withdraw. |
| A18 | "The pipeline finds real effects and makes nothing from noise." (fW:33); "the machinery detects a known effect" (private PHASE4 table) | `candidates/run.py:41-63`: SPY close-to-open return in bp; `C.block_se` (monthly-block bootstrap SD of the mean); one sign-flipped monthly block shuffle with a fixed seed; no stitching, roll, sizing or cost code path. Recomputed: N 6,210, mean 3.042 bp, block SE 0.737, iid SE 0.885, lag-1 autocorrelation −0.096; shuffle t −0.24 (from `C14.md`); preserved diagnostic `audit/diagnostics/d5_c14_stats.py` (+ `.out`) | One daily return series with a known positive mean gives t = 4.13 under `block_se`; one shuffled draw gives t = −0.24. Nothing else in the futures pipeline is exercised. | That this constitutes evidence about "the pipeline". | "On SPY's overnight return, `block_se` and one sign-flipped block shuffle return t = 4.13 and −0.24; the futures stitching, sizing, roll and cost code paths are not involved." "Makes nothing from noise" and any coverage statement: withdraw. |
| A19 | "Checks every number quoted in this README, in WRITEUP.md and in SUMMARY.md against reports/results/, so the prose cannot drift from the run." (bR:56); "Every figure above is reproduced by python run.py and checked … by tests/test_documents.py" (bS:86) | `tests/test_documents.py` (rebuilt with the write-up on 2026-09-12): 52 literal phrase checks rebuilt from `reports/results/`, `config.py` and `src/vol_model.FEATURES` — numbers embedded in words — plus 5 data-derived checks (the leverage-mix correlation and the print-day counts) run only when `data/` is present, and 2 named private figures; run 2026-09-13 in the working tree: "documents: 52 quoted phrases match reports/results/, 0 failures; 5 data-derived phrases match data/"; on a fresh clone without `data/`: "data/ absent, 5 data-derived phrases not checked". `run.py:38-49`: a stage is skipped when its artefact exists unless `--only` or `--force-from` is given | The test checks 52 selected literal phrases (and 5 more when `data/` is present) (digits and the surrounding words) for presence in the documents; it does not validate what the phrases mean. The quoted assertion is the superseded README/SUMMARY text; the current sentence is at bR:20 and bW:21. `run.py` by default skips stages whose result JSON exists; `--only <stage>` and `--force-from <stage>` recompute. | — | "52 selected literal phrases are checked for presence against reports/results/ and config.py, without validating their interpretation; up to five further data-derived phrases are checked when their source files are available. Default `run.py` runs skip stages whose artefacts exist; `--only` and `--force-from` recompute them." "Cannot drift" / "every figure is reproduced": withdraw. |
| A20 | "every one of the 36 cluster touches coincided with real forced selling" (bW:64; bS:17; T:44 "every one of the 36 cluster touches on print-covered days coincided with actual liquidations") | ES `ground_truth`: `cluster {n 36, pct_with_liquidation 1.0}`, `mid {908, 0.999}`, `empty {5164, 0.924}`; `event_study.py:210-218`: `liq_tot = liq_long + liq_short` summed over bars b..b+3 (the touch bar and the next three five-minute bars) for the whole market; `covered = have_liq[b:e].any()`; touches are identified from bar high/low ranges (`event_study.py` collect), so a print inside the touch bar may precede the crossing | On print-covered days, 36 of 36 cluster touches, 907 of 908 mid touches and 92.4 % of empty touches had a market-wide liquidation print within the touch bar or the following three bars. The statistic does not condition on the touched level or its side, and does not order prints relative to the crossing within the touch bar. | That a market-wide print in that four-bar window "coincides with" forced selling at the touched level. | "In the touch bar and the next three five-minute bars, on print-covered days, a liquidation print somewhere in the market occurred for 100 % of cluster touches (n 36), 99.9 % of mid touches (n 908) and 92.4 % of empty touches (n 5,164)." "Coincided" with the level: withdraw. |
| A21 | "mapped price levels carried 5.3x the liquidation notional of unmapped ones" (bR:11; bW:65; bS:18; T:44); "$1.45m against $274k"; "Unmapped means … 94% of the control group. Excluding them gives 2.4x" (bW:67-69) | ES `ground_truth`: `notional_multiple_cluster_over_empty 5.284`; `mean_liq_notional` cluster 1,449,269, **mid 1,537,902**, empty 274,300; `empty_zero_mass_share 0.939`; `notional_multiple_excluding_emptied 2.379`; `config.py:39-40` CLUSTER_SHARE 0.05, EMPTY_SHARE 0.005; sample: all touches on print-covered days across the full grid (`event_study.py:213-218` uses `touches`, not `train_t`); preserved diagnostic `audit/diagnostics/d2_print_days.py` (+ `.out`): 73 OI-covered first-of-month print days, 26 inside the sealed blocks with the ±3-day purge, 47 in the training window | Cluster mean 5.28× empty mean; mid-share levels (0.5–5 %) carry a higher mean than clusters. 93.9 % of "empty" touches are buckets with zero map mass. Excluding those, 2.38×. The sample includes print days inside the sealed blocks. | — | "Mean market-wide liquidation notional in the touch bar and the next three five-minute bars: cluster $1.45m (n 36), mid $1.54m (n 908), empty $0.27m (n 5,164); 94 % of empty touches are zero-mass buckets; cluster/empty = 5.3, cluster/non-zero-mass empty = 2.4. Computed on all 73 OI-covered print days, of which 26 fall inside the sealed blocks including their ±3-day purge." Monotone or "mapped levels carry more" language: withdraw. |
| A22 | "(looks: 0; measured on sample days inside the training window)" (bW:66-67); "(looks: 0; training-window sample days)" (bS:19) | as A21 | The ground-truth sample includes sealed-block days. | — | withdraw. |
| A23 | "the map features carry a few points of R² beyond what a heterogeneous autoregression of past realised variance knows" (bS:53-54); "The same flow reconstruction *does* forecast volatility" (bR:14; bW:94) | `vol_model.py:28-36` FEATURES: 9 map-derived, plus `funding_rate, premium_index, oi_vol_ratio, liq_long_1h, liq_short_1h, liq_long_4h, liq_short_4h, log_rv_1h, log_rv_4h, log_rv_24h, log_garch, log_dvol, hour_sin, hour_cos, dow` (24 total); `har.py:44,52-59`: HAR uses 3 trailing log-RV features; no ablation exists in `har.py` or `vol_model.py` (grep: none) | Model A (24 features) exceeds HAR-intraday on R² by +0.024/+0.026/+0.038/+0.051 in the sealed cells. No artefact isolates the map-derived features' contribution. | That the difference is attributable to the map features rather than to DVOL, GARCH, funding, premium, OI ratio, liquidation counts or time-of-day. | "A 24-feature ridge model including map-derived features, DVOL, GARCH, funding, premium index, OI ratio and time-of-day exceeds HAR-intraday's R² by 0.024–0.051 in the sealed cells." "The map features carry" / "the flow reconstruction forecasts volatility": withdraw. |

## B. btc-public — remaining assertions

| # | exact assertion (file:line) | source | what the source establishes | assumptions | permitted wording |
|---|---|---|---|---|---|
| B1 | "five pre-registered framings" (bR:12-13; bW:72-75; T:44) | private VERSIONS.md: v2 ("the single number: ratio of mean signed 60-minute continuation after a cluster touch to the same after a matched empty touch", VERSIONS.md:105-108), v3 (pressure; sub-hypotheses age, regime), v10 (anticipation, three horizons); public `event_study.py` docstring lists five | Each framing has a private VERSIONS entry with a criterion recorded in a commit preceding its result commit (A5). The registered v2 statistic is a ratio; the public framing-1 statistics (matched difference; absolute-move slope on linear share) are not the registered statistic, and the private absolute-move regression was not registered either (A7). | Commit order ≈ sequence. | "Five framings, each with a hypothesis recorded privately before its result commit; the public statistics for framing 1 are not the registered statistic." |
| B2 | "each named in advance the confound it had to separate, and each was paired with a label-shuffle placebo" (bW:75-76); "each with its placebo and its MDE" (bR:50) | `event_study.py:247-310`: `placebo_labels` called for framings 1 and 5 only; MDEs exist for framing 1 (abs-move slope), 2, 3 (raw), 5; none for framing 1's matched difference or framing 4. Private VERSIONS.md:289-292: "v3 pre-committed to a *shape* without naming the confound that produces the same shape. From v4 the criterion must name what the effect is being separated from" | Two of five framings have placebos. Framings 2, 3 and 4 (all from v3) were registered, by the private record's own statement, without naming the confound; the naming requirement was imposed from v4. | — | withdraw "each named in advance the confound"; withdraw "each was paired with a placebo"; withdraw "each with its MDE". |
| B3 | "Four were underpowered, which is a fact about the design and not a result." (bW:78) | ES: framing 3 raw slope −11.64 [−17.67, −6.44], MDE 8.00 (bps per unit log-age); framing 1 abs-move slope [197, 446] excludes 0; framing 2 slope −0.092 [−0.633, 0.418], MDE 0.753 — outcome `cont_60m` is in bps (`event_study.py:60`), regressor log pressure, so units are bps per unit log-pressure; framing 4 no intervals | Framing 3's raw statistic and framing 1's absolute-move statistic have intervals excluding zero; framing 2 has an interval including zero with MDE 0.75 bps per log-unit; framing 4 has none. "Underpowered" is not established for 1 or 3. | — | withdraw. |
| B4 | "53,088 touches across 61 print-covered days give a minimum detectable effect of 6.5bps against a 10bps band" (bW:79-80; bS:27-28) | ES `5_anticipation.level_after_unpredicted.30m {n 53088, n_days 61, mde_bps 6.54}`; `event_study.py:301-302` `unpred` = rows of `train_p` (prints); recomputed: 47 first-of-month print days in the training window; ES n_days 61 | n counts prints (rows of the print table), not touches. 61 day-blocks against 47 training print days; the extra 14 blocks are days other than the 1st (mechanism not verified in this pass). MDE 6.54 = 2.802 × bootstrap SD of the mean under 61 day-blocks. | That day-blocks are exchangeable and that the extra 14 blocks are spill bars (inferred from `grid.py` tolerance; not verified). | "53,088 prints on 61 day-blocks (47 first-of-month training days); day-block bootstrap MDE 6.5 bps." "touches": withdraw. |
| B5 | "the measured level was −0.3bps, 95% CI [−4.8, 4.0]" (bW:81; bS:28-29) | ES 30m `level_bps −0.32, ci_lo −4.82, ci_hi 4.00`; 15m −0.94 [−3.54, 1.67]; 60m +3.81 [−1.48, 10.27] | The 30m value is as stated; 15m and 60m are also stored; private v10 reported all three and named no primary. | — | "At 30 min −0.3 [−4.8, 4.0]; at 15 min −0.9 [−3.5, 1.7]; at 60 min +3.8 [−1.5, 10.3] (day-block bootstrap, print unit)." Quoting 30m alone: withdraw. |
| B6 | "Its matched difference cleared its placebo comfortably — real −7.1bps against a shuffled 95th percentile of 1.1bps" (bW:82-83) | ES `5_anticipation.placebo {real −7.069, placebo_mean −0.018, placebo_abs_p95 1.135, clears true, n 300}`; `audit.py:132-146`: labels redrawn i.i.d. per print at the observed rate; `p95 = percentile(|draws − centre|, 95)` (absolute deviation from the placebo mean); real difference uses `min_cell=15`, placebo draws use `min_cell=3`; `audit.py:101-102` (block_shuffle docstring): i.i.d. shuffles "far too easy to pass" | The public placebo value is 1.135 (rounds to 1.1); the placebo null is independent per-print relabelling; the 95th percentile is of absolute deviations from the placebo mean; the real and placebo computations use different minimum cell sizes (15 vs 3). | That per-print independent relabelling is the right null for prints sharing a bar and a day. | "The matched difference is −7.1 bps; under independent per-print relabelling (min cell 3; 300 draws) the 95th percentile of absolute deviations from the placebo mean is 1.1 bps; the real difference uses min cell 15." "Cleared comfortably": withdraw. |
| B7 | "The randomisation test on the level itself could not distinguish the measured value from shuffled labels, which is what a real null looks like." (bW:83-85) | `event_study.py:314-317`: `audit.randomisation(nanmean, cont_30m, day)`; `audit.py:98-115` `block_shuffle` permutes day-blocks of the same values, truncating to the shorter block; ES `randomisation {real −0.315, placebo_mean 1.563, placebo_abs_p95 4.03, clears false}` | The statistic is a mean of the same values with some dropped by truncation; the placebo distribution reflects which prints are dropped, not label exchangeability. | — | withdraw. |
| B8 | "Adequate power and a null is a finding." (bW:85); "REFUTED with power" (private v10) | ES 30m MDE 6.54 < band 10; CI [−4.82, 4.00] | The design's MDE is below the band and the interval contains zero and lies inside ±10. | Day-block exchangeability; print unit; that ±10 bps is the relevant margin. | "The 30-minute interval [−4.8, 4.0] lies inside ±10 bps; the design's MDE is 6.5 bps." No "finding" beyond the interval. |
| B9 | "Every input to the map — open interest, price, funding — is public and watched. Anyone who wants to know where the liquidations sit already knows. The flow arrives exactly where the map says it will and is absorbed at the price the order book was already showing." (bW:87-90); "the information is already in the price" (bW:180) | Private v10 (VERSIONS.md) tested two implications of anticipation — continuation after unpredicted vs predicted prints (matched difference −7.07, CI [−23.45, +5.94], MDE 21.4 > band: inconclusive) and a cross-venue prediction (OKX +16.03 bps, MDE 27.0, CI [−0.6, +27.2]) — and private v3 reported depth withdrawing ahead of mapped levels | Implications of the anticipation hypothesis were tested privately and returned inconclusive results. No artefact identifies anticipation as the cause of the absent price response. | — | withdraw the affirmative mechanism. (Permitted: "anticipation was tested privately through two implications; both were inconclusive.") |
| B10 | "sealed-block R² of 0.457 against 0.311 and 0.554 against 0.165 at one hour, 0.464 against 0.232 and 0.540 against −0.233 at four hours … comfortably against a GARCH(1,1) baseline" (bW:97-99; bS:39-41; bR:16) | VM `scores`; `vol_model.py:47-50` arch_model(...).fit(disp="off", show_warning=False) on all training rows once; VM `garch {alpha 0.2, beta 0.78}`; arch 7.2.0 `GARCH.starting_values` grid: alphas [.01,.05,.1,.2] × abg [.5,.7,.9,.98]; VM QLIKE: GARCH lower (better) in 1h/2022, 1h/recent, 4h/2022 | R² values are as stated. The fitted GARCH parameters equal a starting-grid point to four decimals; convergence status is not preserved in the reported output. On QLIKE the GARCH forecast is better in three of four sealed cells. | Whether the optimiser converged: unknown (a grid point can be an optimum; the audit's "did not move" is a reviewer inference, not verified). | "R² as stated; the GARCH parameters coincide with arch's starting-value grid and convergence status is not preserved in the reported output; on QLIKE GARCH is better in three of four sealed cells." "Comfortably": withdraw. |
| B11 | "Corsi's standard day/week/month cascade loses by 0.13 to 0.22 R² in the sealed blocks" (bW:103-104) | HAR `gain_over_har_dwm` sealed: 0.155, 0.220, 0.132, 0.187 (from run log) | Range 0.13–0.22 as stated. | — | as stated. |
| B12 | "Model A still wins in both sealed blocks at both horizons, but by +0.024 and +0.026 at one hour and +0.038 and +0.051 at four" (bW:109-110; bS:49-51) | HAR `gain_over_har_intraday`: 0.0237, 0.0258, 0.0383, 0.0512; no SE/CI/test in HAR or code; VM `n` 87,552 / 87,264 (= 304 / 303 complete-day equivalents at 288 bars/day); `vol_model.py:78-87` slices skipped when < 20,000 training rows (E3) | Point differences as stated, on 87,552 and 87,264 evaluated rows per block (304 and 303 complete-day equivalents; actual distinct-day coverage requires the complete-day assumption or preserved timestamp membership), with no uncertainty computed. | — | "Point R² differences of +0.024/+0.026/+0.038/+0.051 on 87,552 and 87,264 evaluated rows per block (304 and 303 complete-day equivalents out of 365); no uncertainty was computed." "Wins": withdraw. |
| B13 | "on QLIKE … the intraday HAR beats Model A in three of the four sealed cells" (bW:111-112; bS:51) | HAR `sealed_cells_lost_on_qlike.har_intraday 3`, `n_sealed_cells 4` | as stated | — | as stated. |
| B14 | "No hold-out counter moved to establish this: HAR was fitted on training rows only and scored against outcomes the single existing look had already revealed." (bW:113-114) | `har.py` (HAR fitted on training rows; Model A predictions read from `model_a.parquet`); `vol_model.py:176`, `har.py:110` `"looks": 0 if label == "train" else 1`; S0 0.4 inspection history | HAR is fitted on training rows. There is no counter to move: the value is a literal. The sealed outcomes have been scored on 2026-09-02 (twice), 09-06, 09-07, 09-12 (twice). | — | "HAR was fitted on training rows and scored on the sealed outcomes; the repository has no look counter; the sealed blocks have been scored repeatedly (S0 0.4)." "No counter moved" and "single existing look": withdraw. |
| B15 | "Each sealed-block number in SUMMARY.md carries the number of times that block was looked at to produce it. The live forward window was never opened." (bR:60-62); "Each was opened exactly once" (`config.py:26`) | as B14; grid ends 2026-09-01; `LIVE_FORWARD_START 2026-09-01` | The "looks" values are literals; the window after 2026-09-01 has no rows. | — | "The window after 2026-09-01 contains no data and has not been scored." The counter sentence: withdraw. |
| B16 | "a histogram of expected forced flow per price bucket, which decays with a seven-day half-life" (bW:35); README table: "decaying histogram" | `config.py:37` `MAP_HALF_LIFE_BARS = 7×24×12/2` = 1,008 bars = 3.5 days | The configured half-life is 3.5 days. | — | "3.5-day half-life". "seven-day": withdraw. |
| B17 | "The leverage weights are not assumed. They are calibrated against real tick-level liquidation prints, on a log scale" (bW:36-38; bR:47) | `liquidation_map.py:194-205`: `fit_mask = training_mask & have`; `calibrate(...)` once; `leverage_mix.json {mix 5x 0.0, 10x 0.2, 20x 0.25, 50x 0.55; corr 0.297; n_fit_days 94}` | One calibration over all training print days (2020-09 → 2025-08, sealed excluded), applied to every period including the 2022 block; log-scale correlation 0.30. | — | "Calibrated once on training print days (log-scale correlation 0.30) and applied to all periods, including the 2022 sealed block." |
| B18 | "a five-minute panel spanning 2020 to 2026 in which a value may appear at time t only if it was publicly knowable at t … An automated test rebuilds sampled grid values" (bW:24-30; bR:46,54) | `tests/test_asof.py:40-72`: for 20 random timestamps and each manifest column, recomputes the value from the raw parquet using the raw file's own `asof_time` column and the manifest tolerance, and compares to the grid; the "forward-target" check verifies that the last H bars of each target column are null | The test checks join consistency between the grid and the raw files' supplied `asof_time` values at 20 sampled timestamps (380 checks, 0 failures on 2026-09-12), and that targets are null at the series tail. It does not establish that the supplied `asof_time` values are the times at which values became publicly knowable, and does not independently reconstruct target direction. | — | "An automated test checks, at 20 sampled timestamps, that grid values match the raw files under the raw files' own as-of timestamps and tolerances, and that forward targets are null at the series tail." "Publicly knowable" and "rebuilds": withdraw as descriptions of what the test establishes. |
| B19 | "The edge, when it was finally found, was about six basis points per trade against a ten-basis-point round trip … after 82,152 feature variants" (bW:52-54; bS:5-8; fW:7) | private v1 only; `test_documents.py:579` names both as uncheckable | Not in the public repository. | — | May be quoted only as "from the private v1 record, not reproducible here". |
| B20 | "Five things it caught" — sign inversion, stale price, infinity poisoning, walk-forward mask, clock feature (bW:148-176) | private `DECISIONS.md` entries: "A single infinity silently destroyed a third of the variant space" (line 325), "`loss_move_down` was assigned the up-move magnitude" (368), "Every as-of join carries a staleness tolerance" (422, the stale-price episode), the walk-forward-mask disclosure (454), "v12 — News adds nothing to Model A, and the staleness clause caught why" (1404) | All five episodes are documented in the private decision log as dated entries. Their causal descriptions (what would have happened without each catch) are the author's account. | — | "Five episodes documented in the private decision log"; the consequences attributed to each are the author's retrospective account and may be reported as such. |
| B21 | "This repository is a consolidation of a larger private project. It reproduces four results" (bR:65-66) | S0 0.5: public values differ from private (v1 gains, v13 QLIKE 2 of 4 vs 3 of 4, idealised MDE 77.7 vs 81.6) | The public code re-implements four analyses; its outputs differ from the private ones. | — | "re-implements four analyses of a private project; its figures differ from the private figures". "Reproduces": withdraw. |
| B22 | "Every source used here is public and keyless" (bR:37-38) | `src/fetch.py` URLs (Binance Vision, Tardis datasets, Deribit public API); option fills: parquet not producible by `fetch.py` as written (S0 0.5) | URLs are public endpoints. Whether the option-fill parquet on disk came from them on the stated schedule: unknown. | — | "The fetch code uses public keyless endpoints; the option-fill file in the archive was not produced by that code as written." |
| B23 | "Trading more often buys nothing, because overlapping trades are not independent observations" (bW:134-135) | `instrument.py:436-437` comment; no computation | A stated rationale; nothing computed. | — | May be stated as an assumption of the years-needed arithmetic. |

## C. futures-public — remaining assertions

| # | exact assertion (file:line) | source | what the source establishes | assumptions | permitted wording |
|---|---|---|---|---|---|
| C1 | "Twelve pre-registered versions said no" (fW:7) | private btc VERSIONS.md: 13 versions (v1–v13), of which v4–v9, v11–v13 are vol/news/HAR/instrument versions, not price-response tests | The count of versions that tested price response is not twelve by the private record (v2, v3, v10 test price response; v1 the combined system). | — | withdraw. |
| C2 | "The modelled round trip across 27 liquid markets is 2.7 basis points at the median, about 4% of a typical daily move" (fW:9); "Expected edge at the band midpoint against modelled cost was 2.3 [training; computed before any return was read]" (fW:15; RESULTS §1) | `stages/s1.py:16` `ASSUMED_SHARPE = 0.4` ("midpoint of SPEC §8 standalone band"); `:44` `edge = ASSUMED_SHARPE × VOL_TARGET / turnover`; `:58-60` per-market notional and dollar-vol medians over `tail(250)`; `config.py` spreads in ticks, impact 0.5 tick; `s1_stats.json` ratio 2.3, median round trip 2.69 bp, 4.1 % of a daily move | The numerator is a projection under an assumed gross Sharpe of 0.4, not a measured edge. Per-market cost percentages use the final 250 observations of price and dollar volatility. | — | "Under an assumed gross Sharpe of 0.4 and the specified cost and schedule model, the projected edge/cost ratio is 2.3; the median modelled round trip is 2.7 bp, 4.1 % of the median daily dollar move over the final 250 observations." |
| C3 | "verified against NYMEX settlements republished by EIA (98.4–98.8% of days match from 2002; 91–96% of expiries are held to the last trading day)" (fW:13) | `candidates/splice_check.py:67-95`: `match_c1 = |yahoo − c1| ≤ TOL`; `held_to_expiry = match_c1[E] and not match_c2[E]` on expiry dates that are bars in the joined series; `share_days_equal_c1_2002on` = mean of `match_c1` over dates with a Yahoo and a C1 observation; `SPLICE_CHECK.md`: CL .9846 NG .9837 HO .9878 RB .9881; held_to_expiry_2002on .9625 .9147 .9529 .9535; four markets | For CL, NG, HO and RB, on 98.4–98.8 % of dates from 2002 with both a Yahoo close and an EIA C1 settlement, the Yahoo close is within tolerance of C1; on 91–96 % of exchange-rule expiry dates that are bars in the joined series, the Yahoo close matches C1 within tolerance and does not match C2. "Held to expiry" is inferred from these price matches, not from observed contract identity. | Tolerance-based price matching identifies the contract. | "For the four energy contracts, on 98.4–98.8 % of eligible dates from 2002 with available observations the Yahoo close matches the EIA C1 settlement within tolerance; on 91–96 % of eligible expiry dates it matches C1 and not C2, from which holding to expiry is inferred." |
| C4 | "every contract switch leaks the calendar spread as a fake return, 11% a year in natural gas and −12% in gasoline" (fW:13) | `s0.md`/`s0_stats`: NG removed artefact 10.98 %/yr; RB −11.9 % (from DECISIONS) | as stated. | That the removed switch-day change is entirely artefact (2.5: it also removes real movement). | as stated with "removed switch-day changes sum to". |
| C5 | "Carry was unmeasurable: Sharpe 0.08 on the 13 covered markets with a minimum detectable effect of 0.61" (fW:21; fR:10) | `s3_stats.json`: sharpe_net 0.08, mde_sharpe 0.61; `backtest.py:70-79`: `se = block_bootstrap(net).std()`; `mde_sharpe = mde_from_se(se)/net.std()×√ANN` | 0.61 is 2.80 × (block-bootstrap SD of the daily mean) ÷ observed daily SD × √252 — a mean-return MDE expressed in Sharpe units. It is not an interval for the Sharpe ratio. | Monthly blocks capture the dependence. | "net Sharpe 0.08; the design's mean-return MDE in Sharpe units is 0.61". "unmeasurable": withdraw. |
| C6 | "Value was actively negative: Sharpe −0.44, −31.8 basis points gross per side" (fW:21; fR:11 "value is refuted (−0.44)") | `s5_stats.json`: −0.44, −31.8; mde_sharpe 0.57 (same construction as C5) | Point estimate as stated; no Sharpe interval exists. | — | "net Sharpe −0.44; no interval computed". "Refuted": withdraw. |
| C7 | "two disagreeing signals shrink the book toward zero, the vol layer levers the residual to about twice, and the doubled costs eat what trend made" (fW:23) | `reports/stage_reports/s6.md:9`: "portfolio scale factor λ median 2.01 (above 1 means the signals cancel and the vol layer levers the residual)"; no artefact records a cost doubling or a causal decomposition | The combined book's reported median portfolio scale factor is 2.01. Nothing measures "doubled costs" or attributes the outcome to them. | — | "The combined book's reported median portfolio scale factor λ is 2.01." The remainder of the sentence: withdraw. |
| C8 | "Combined … Sharpe 0.06, realised vol 16.6% … maximum drawdown −53%, effective breadth 8.6 bets from 27 markets, trend contributing +0.45 and value −0.28" (fW:25) | `s6_stats.json`; `portfolio.py:96` `neff = (Σ|risk|)²/var`; `backtest.py:84-86` `max_drawdown`: `eq = cumprod(1+r)`, `peak = cummax(eq)` (initial 1.0 not included); `s6.py:68-72` `contrib[k] = sharpe_net(combined) − sharpe(book rebuilt without signal k)` | Values as stated. `neff` is the squared ratio of summed standalone risk to portfolio variance-implied risk. Max drawdown is measured from the running peak of the equity curve starting after the first return. The "contributions" are leave-one-signal-out Sharpe differences (combined Sharpe minus the Sharpe of a separately rebuilt book without that signal), not additive P&L contributions. | — | "diversification ratio squared 8.6"; "max drawdown −53 % (from the first running peak, not from initial capital)"; "leave-one-signal-out Sharpe differences: trend +0.45, value −0.28". "8.6 bets" and "contributing": withdraw. |
| C9 | "the 40-draw placebo's 95th percentile is 0.05–0.08 depending on the data vintage, so the book is indistinguishable from its placebo" (fW:25); RESULTS §5 "placebo cleared (p95 0.05)" | `s6.py:21` PLACEBO_N 40; `:88-93` p95 = `nanpercentile(plc, 95)`, `cleared = sharpe_net > p95`; public RESULTS p95 0.05 vs 0.064; private commit `b3dc344` p95 0.08 | With 40 draws the 95th percentile is interpolated between the 2nd and 3rd largest draws. In the public run the observed 0.064 exceeded the implemented threshold (0.05); in the private vintage it did not (0.08). | — | "The public run's combined net Sharpe (0.064) exceeded its implemented 40-draw placebo threshold (0.05); the private vintage's did not (0.08)." That is an operational result under the implemented procedure, not a validated significance claim; "indistinguishable" as a verdict: withdraw. |
| C10 | "The sealed hold-out: −1.28, annual return −21%, with trend −0.70, carry +0.69, value −0.72 [… two-year standard error 0.71]" (fW:25; fR:11) | `s9_stats.json` / RESULTS §9; `s9.py:50` `se2 = sqrt((1 + 0.5 × S_train²)/2)` with the *training* Sharpe (0.064) and a fixed two-year duration, used only in the consistency check `|S_holdout − S_train| ≤ 2 × se2` | The hold-out estimates are as stated. 0.71 is the consistency check's calculation from the training Sharpe and T = 2; it is not an estimated standard error of the observed hold-out Sharpe. No interval for the hold-out Sharpe exists in the artefacts. | — | "Hold-out net Sharpe −1.28 (annual return −21 %; trend −0.70, carry +0.69, value −0.72). The figure 0.71 is the s9 consistency check's training-based calculation, sqrt((1 + 0.5 × 0.064²)/2); no interval for the hold-out Sharpe has been computed." "Two-year standard error": withdraw. |
| C11 | "Opened once on 2026-09-09 in the original run. The reproduction on 2026-09-12 re-derives the same number …" (fW:3, added this session); fR:43-46 | private `holdout_looks.json` (11:43 aborted registration; 11:45 scored, −1.18); public `holdout_looks.json` (09-12 04:53, −1.28); S0 0.4 | The block was scored on 2026-09-09 (−1.18, partial bar) and 2026-09-12 (−1.28, settled); an aborted run registered a look before scoring. "The same number" is not what was produced (−1.18 vs −1.28). | — | "Scored 2026-09-09 (−1.18 on a partial final bar) and 2026-09-12 (−1.28 on the settled close); an aborted run had registered a look at 11:43 on 09-09 without scoring." "Opened once" and "the same number": withdraw. |
| C12 | "The ranking was committed to git before any test as a prediction to be scored. Then all twenty criteria were pre-registered at once … so that a directional claim clears only at t ≥ 3.02" (fW:29; fR:13) | private commits `429601d`, `7145117`, `bb9f1b9`, `ed9498a` (S0 0.3); `common.py:27` Z_CLEAR 3.02 = two-sided 0.05/20 | Commit order as recorded in a local unpushed repository; not execution order; not in the public repository. 3.02 is the two-sided cutoff at α = 0.0025. | Commit messages describe contents; author dates are accurate. | "Recorded in a private local repository in commits preceding the result commits; not verifiable from the public repository; commit order, not execution order." |
| C13 | "One candidate cleared the corrected threshold: crypto funding-rate carry … alpha over Bitcoin of +1.02% per week, standard error 0.27, t = 3.73, placebo 95th percentile +0.40" (fW:35; fR:15) | `C18.md`; `common.py:382-400` (OLS intercept on weekly BTC return; month-block bootstrap SE); `run.py:196-219`: 400 draws, each week's long and short names drawn uniformly at random from that week's eligible symbols, book returns recomputed with the same price and funding data, alpha on BTC recomputed; 95th percentile of the resulting alphas +0.399 | Gross alpha, SE and t as stated. The placebo randomises which symbols enter each leg each week, removing selection on trailing funding; the 95th percentile of the resulting alphas is +0.40. Whether that is the right null for the claim depends on the exchangeability of symbols within a week under the null, which the artefacts do not address. | Month blocks capture weekly dependence. | "Gross alpha +1.02 %/wk, month-block SE 0.27, t 3.73. Placebo: 400 books with symbols assigned at random within each week; 95th percentile of their alphas +0.40." Whether the result would clear with costs in the series: unknown. |
| C14 | "The audit was pre-registered because the result sat above its plausible band: five refutation clauses … and none fired" (fW:35) | `C18_AUDIT.md` verdict; `audit_c18.py:54-58`; `PREREG.md` C18 section (placebo and refutation clause "alpha < threshold, or the placebo reproduces it"); the five clauses appear in `DECISIONS.md` 2026-09-11 and `audit_c18.py` (committed `b7cd128`, after the result) | The five clauses were run and none fired. The registration file lists a different (single) refutation clause; the five-clause audit is first recorded in the commit that records the cleared result. | — | "Five post-hoc audit clauses (recorded with the result) did not fire." "Pre-registered": withdraw. |
| C15 | "The premium fell fourfold after 2021: +2.25% per week in 2020–21, +0.55% from 2022 on, and in that later period the price leg is zero (+0.08%, t 0.4) … The post-2021 sub-sample alone, t = 2.69, would not clear at N = 20" (fW:35) | `C18_AUDIT.md` table: 2020-02→2021-12 +2.249 (SE 0.827); 2022+ +0.547 (0.204, t 2.69); price leg 2022+ +0.081 (0.204, t 0.40) | As stated (gross, before costs). | — | as stated with "gross" and "point estimates". "Is zero": withdraw (estimate +0.08, SE 0.20). |
| C16 | "Survivorship — only perpetuals still listed — is a limitation of unknown sign." (fW:35) | `candidates/fetch.py:60-63`: `status == TRADING` and `onboardDate ≤ 2023-09-01`; recomputed: 121 of 142 symbols first bar after 2020-02-17 | The universe is survivor-conditioned. The direction of bias is not computed anywhere. | — | as stated. |
| C17 | "The first run was void: the fetcher had kept only the most recent 500 funding rows per symbol … Logged, fixed, re-run with the criterion untouched." (fW:35) | private `LOG.md` 17:24 NOT CLEARED t 0.70, 17:25 VOID; `C18_void_truncated_funding.md`; `5b2801a` diff (fetch.py; PREREG C18 design line SE 0.033→0.272, clearing alpha 0.099→0.822) | Logged privately; the public `LOG.md` (regenerated) lacks it. The statistic, placebo and clause text are unchanged; the numeric threshold and design SE were recomputed. | — | "Logged in the private record; the numeric clearing threshold was recomputed from 0.099 to 0.822 %/wk after the re-fetch." "Criterion untouched": permitted only with that qualification. |
| C18 | "A VIX-regime exposure cap, ranked first, moved the daily-loss breach share by −0.5 points, t = −0.32" (fW:37) | `C19.md`; `run.py:64-76`: differences of overlapping 60-day-window outcomes, monthly `block_se` | As stated; SE from monthly blocks on 60-day overlapping windows. | Monthly blocks shorter than the window length; SE likely understated. | as stated with "monthly-block SE on overlapping 60-day windows". |
| C19 | "Expiring-contract pressure, ranked second, returned the wrong sign, +0.05σ, t = 0.87" (fW:37) | `C2.md` | as stated. | — | as stated. |
| C20 | "Skewness premium: slope +0.18, t = 2.12 … Roll-gap carry … +0.20, t = 2.48 … The Treasury auction concession: +0.17σ, t = 1.72 … Post-EIA-report drift … +0.06σ, t = 1.58" (fW:41) | `C9.md`, `C1.md`, `C11.md`, `C15.md` | as stated. C15 was run after the clear-halt (A3). | — | as stated; C15 with the halt note. |
| C21 | "Every sign was the predicted one at roughly the published magnitude; every t fell between 1.6 and 2.5 against a bar of 3.02; two would have cleared without the correction" (fW:41) | candidate reports; `PREREG.md:108` (predictions); `C9.md`, `C1.md`: "5% unadjusted yes"; no artefact records published magnitudes | The estimates for C9, C1, C11 and C15 have the registered sign; C2's does not. No reference magnitudes exist in any artefact. C9 and C1 exceed the unadjusted 5 % cutoff and not the registered N = 20 cutoff. | — | "C9, C1, C11 and C15 returned the registered sign; C2 did not. C9 (t 2.12) and C1 (t 2.48) exceed the unadjusted 5 % cutoff and not the registered corrected cutoff of 3.02 — an explicitly unadjusted diagnostic, not a clearance." "Published magnitude": withdraw. |
| C22 | "At the observed slopes the futures panel would need about 2.8 times its sample to clear." (fW:41) | PHASE4_REPORT: (3.86/2.3)² ≈ 2.8 | Arithmetic: the sample multiple at which 80 % power would be reached at the N = 20 threshold if the observed slope were the true effect, the SE fell as N^(−1/2), and the added data had the same distribution and dependence as the existing panel. | Observed = true effect; SE ∝ N^(−1/2); stationarity of distribution and dependence over the extension. | "If the observed slope were the true effect, the standard error fell as N^(−1/2), and the additional data resembled the existing panel, 80 % power at the N = 20 threshold would need (3.86/2.3)² ≈ 2.8 × the sample." |
| C23 | "The one clear came from 342 weeks across roughly 110 instruments, 29 names a leg." (fW:45); "~110 instruments" (T:49) | `C18.md` mean_symbols 108.5; recomputed names/leg mean 22.2, max 29 | 342 weeks; mean cross-section 108.5; 22 names per leg on average, 29 at most. | — | "342 weeks, ~109 symbols, 22 names per leg on average (max 29)". "29 names a leg": withdraw. |
| C24 | "Every published premium replicated in sign: trend, carry, skewness, roll-gap carry, the auction cycle, the report drift, the overnight control and funding carry." (fW:49); fR:16-17 "every futures premium showed the predicted sign at roughly its published size" | `s2/s3/s5_stats.json` (trend +0.24, carry +0.08, value −0.44), `C9.md`, `C1.md`, `C11.md`, `C15.md`, `C14.md`, `C18.md` (positive estimates); `C2.md` (wrong sign); no reference magnitudes | The eight named estimates have positive point estimates. Value (a published premium tested in the same programme) and C2 have the opposite sign to their predictions and are omitted from the list. "Published size" has no reference. | — | "The point estimates for trend, carry, C9, C1, C11, C15, C14 and C18 have the predicted sign; value and C2 do not." "Every" and "at roughly its published size": withdraw. |
| C25 | "Of three candidates predicted to clear, none did; both predicted 'right sign, not cleared' were right; the clear came from a candidate predicted to fail on power." (fW:49) | `PREREG.md:108` (as committed `bb9f1b9`): "Expected to clear at N = 20: C14 (control, expected), C19, C2 (now borderline), C9. Expected sign-right-not-cleared: C1, C11, C18, C15"; results per `LOG.md` | Of the three non-control candidates predicted to clear (C19, C2, C9), none cleared. Four candidates were predicted "sign right, not cleared" (C1, C11, C18, C15); three matched that prediction; C18 cleared. | — | "None of the three non-control candidates predicted to clear cleared; three of the four predicted sign-right/not-cleared matched that prediction; the fourth, C18, cleared." "Both": withdraw. |
| C26 | "Of 500 mandates … 53 are attainable … The daily loss limit binds in 68% of mandates, the drawdown limit in 28%, and the time window in 4%" (fW:53; fR:18-19) | `s8.py:56-64`: for each mandate, `best` = the tested vol target with the highest pass probability; `attainable = pass_prob ≥ ATTAINABLE (0.5)` at that target; `binding = idxmax` over the shares of `breach_dd`, `breach_daily`, `timeout` at that target; `share_bind` = distribution of `binding` over mandates; `s8.md:3,15` | "Attainable" means: at its best tested volatility target, at least 50 % of overlapping historical start dates reach the profit target before any limit. 68/28/4 classify each mandate by which non-pass outcome is most common at that selected target; they are not the fraction of attempts ending each way, nor a counterfactual test of which constraint prevents success. Training path; drawdown accounting per C8. | — | "Of 500 specified mandates, 53 reach a pass probability of at least 50 % at their best tested volatility target on the training path. Classifying each mandate by its most common non-pass outcome at that target: daily-loss breach 68 %, drawdown breach 28 %, timeout 4 %. Historical, overlapping starts, before the drawdown-accounting correction." "Binds": withdraw. |
| C27 | "the pass-maximising volatility target is 25%, while the target that maximises return over drawdown is 40%: the gap is −15 points, opposite in sign to the prediction" (fW:53) | `s8.md:51-53`: Calmar by vol, to two decimals: 0.02 at 5–30 %, 0.03 at 40 % (the top of the tested grid); pass-max 25 % for the SPEC example mandate | On the tested grid the Calmar-maximising target is 40 % (the grid's upper endpoint; the surface is 0.02 at every lower target to the report's two-decimal precision) and the pass-maximising target for the example mandate is 25 %; the difference on that grid is −15 points. An endpoint maximum does not support a claim about an unrestricted optimum. | — | "On the tested grid (5–40 %), the pass-maximising target for the example mandate is 25 % and the Calmar-maximising target is 40 %, the grid's upper endpoint; Calmar is 0.02 at every lower target to two decimals. The selected targets differ by −15 percentage points on that grid." Any statement about an unrestricted optimum: withdraw. |
| C28 | "this path, Sharpe 0.06, passes 22.3% of the time at 15% vol, against the specification's synthetic prior of 23% for a Sharpe of 0.8" (fW:53) | `s7.md:3`, `s7_stats.json`: 22.3 % of overlapping historical starts pass after the full path is rescaled to 15 % realised volatility; private `SPEC.md:321-323`: "Preliminary synthetic modelling suggests a strategy with a Sharpe of 0.8 run at 15% volatility has roughly a 23% chance …"; the synthetic calculation is not in any artefact | The historical simulator reports 22.3 %. The 23 % is an assertion in the specification whose underlying synthetic calculation and comparability are not recovered. | — | "The historical simulator reports a 22.3 % pass rate for the example mandate at 15 % vol; the specification states an approximately 23 % synthetic prior for a Sharpe of 0.8, whose calculation has not been recovered." |
| C29 | "That is a power failure, not an absence, and the project can prove the distinction rather than assert it." (fW:63) | A18, A2, C21 | Nothing in the repository proves a distinction between power failure and absence. | — | withdraw. |
| C30 | "a 35% sector cap with the effective number of independent bets reported" (fW:17) | `portfolio.py:60-69`: each sector whose standalone-risk share exceeds the cap is scaled down by cap/share in one pass; no renormalisation; `stages/s4.py:60` and `s4.md:21-30` report the s4 book's realised sector risk shares (mean/min/max): ags 0.282/0.063/**0.446**, energy 0.187/0.033/**0.365**, equity 0.113/0.005/0.238, fx 0.156/0.047/**0.351**, metals 0.139/0.029/0.289, rates 0.123/0.004/0.292 | The cap is applied once without renormalisation. For the s4 book the reported maximum sector shares exceed 35 % for agriculture (44.6 %), energy (36.5 %) and FX (35.1 %). The maxima for the s6 combined book are reported in RESULTS §4 (ags 45 %, energy 36 %, fx 35 %). | — | "Sectors above a 35 % standalone-risk share are scaled down once by cap/share; realised sector shares reach 44.6 % (ags), 36.5 % (energy) and 35.1 % (FX) at their maxima in the s4 book." "35 % sector cap" without that qualification: withdraw. |
| C31 | "the whole study re-run under five roll treatments and two adjustment methods as a gate" (fW:13); `s0_stitch.py` offsets | `s0_stitch.py:88-95`: `offset_scan(d, exp, z)` over the full series `d` (min to max index, including the sealed period); `stages/signal_eval.py` and `s6.py` stitching sensitivity: each signal's and the combined programme's Sharpe recomputed on the roll-treatment variants (raw / w0 / w1 / w3 / early3) and on the Panama and ratio series | Two separate facts. (i) Roll-switch offsets are selected on the full price history including the sealed period. (ii) The sensitivity code recomputes the specified signal and combined-programme Sharpes under the roll-treatment variants; it does not re-run every stage (costs, portfolio construction, simulator, sweep) under each treatment. | — | "Roll-switch offsets were selected on the full price history including the sealed period. The stitching sensitivity recomputes each signal's and the combined programme's Sharpe under five roll treatments and two adjustment series." "The whole study re-run": withdraw. |
| C32 | Fresh-clone build and hold-out retention: historical and current behaviour | Before 2026-09-13 the tracked progress file caused a fresh clone to skip the build and the candidate chain failed; a forced run halted at the s9 look counter. The first repair ran all stages when w0 was absent and retained s9. Current `build.py` validates stage outputs and invalidates downstream checkpoints; `stages/s9.py` requires nonempty retained RESULTS.md and s9.md before recording retention. See I10/I15 and REPRODUCTION.md. | An inspected hold-out is retained, not scored again. Missing required inputs or retained reports stop or rebuild as specified by the current guards. | Network acquisition can fail; local guard tests do not establish universal reproduction success. | State which outputs were generated and which retained. On a fresh clone the training stages rebuild; s9 retains validated historical reports without scoring, or stops when they are missing. Do not present retained RESULTS.md as a freshly generated summary. |

## D. Top-level README (P7/P8 material) — assertions not already covered

| # | exact assertion (file:line) | source | what the source establishes | assumptions | permitted wording |
|---|---|---|---|---|---|
| D1 | "Reconstructs where leveraged BTC perpetual positions will be force-closed … and finds the map is real but not tradeable: forced flow arrives where it predicts, price does not respond" (T:44) | A20, A21, B5, B8 | Market-wide prints follow touches at a high base rate for all touch types. Post-print continuation after unpredicted prints, print unit, day-block intervals: 15m −0.94 [−3.54, 1.67]; 30m −0.32 [−4.82, 4.00]; 60m +3.81 [−1.48, 10.27] bps. | As A20/A21/B5. | "Liquidation prints occur in the touch bar and next three bars for 100 % of cluster touches and 92 % of unmapped touches; continuation after unpredicted prints is −0.9 [−3.5, 1.7] bps at 15 min, −0.3 [−4.8, 4.0] at 30 min and +3.8 [−1.5, 10.3] at 60 min (print unit, day-block bootstrap)." "The map is real", "price does not respond", and any statement that all horizons are bounded within ±10 bps: withdraw. |
| D2 | "the one component that generalises — a volatility forecast — is worth less than the cheapest instrument that could express it" (T:44; bR:17-18) | A11–A16 | Derived from the economics chain with its listed defects. | — | withdraw. |
| D3 | "P7 asked whether an effect exists … and failed on economics: the method held, but six basis points of edge against a ten-basis-point round trip made the trade impossible" (T:41) | B19 (private v1 only) for the six-bps figure; no artefact for "the method held" or "made the trade impossible" | The six-bps and ten-bps figures are the author's private v1 record, not reproducible here. "The method held" and "made the trade impossible" have no separate support. | — | The six-bps-against-ten figure may be stated only as the author's account from the private record. "The method held" and "made the trade impossible": withdraw. |
| D4 | "P8 … failed on power, with every premium showing its published sign but none clearing a twenty-way correction on 27 markets. The third, equities, follows because breadth turned out to be the binding constraint." (T:41) | C21, C24, A2, C22 | Four of five futures candidates and six of eight named estimates have the predicted sign; C2 and value do not. None of the futures candidates met the registered corrected threshold. "Failed on power" as a diagnosis rests on the conditional extrapolation in C22. Breadth as cause: no artefact. | — | "None of the futures candidates met the registered corrected threshold; C9, C1, C11 and C15 have the predicted sign, C2 and value do not." "Failed on power" is permitted only with C22's conditional statement beside it. "Every premium" and "breadth turned out to be the binding constraint": withdraw. |
| D5 | "P7's liquidation map infers position cohorts from open-interest changes rather than observing positions, so the leverage mix is fitted rather than known. The calibration is against real liquidation prints, but the inference step is an assumption." (T:82) | `liquidation_map.py` (ΔOI cohorts; calibration on prints, B17) | as stated. | — | as stated. |
| D6 | "P8 runs on free daily data with front-month splices that had to be located and neutralised rather than avoided, and 27 markets is too few to resolve most of the premia it tests — which is the project's own finding." (T:83) | C3, C4, C20; "too few to resolve" = design MDEs above the effects (C5, C6, C22) | Splice treatment as stated. "Too few to resolve" restates that the t-values were below 3.02 at the registered breadth. | — | "…and the registered tests on 13–27 markets did not reach the corrected threshold." "Too few to resolve" as a finding about breadth: withdraw (A2). |
| D7 | "P6's pseudo-labelling trains on the model's own confident predictions … At 0.52 accuracy, rank is partly noise." (T:81) | `p6_qrt_challenge/README.md` (self-described method); no code | Method as self-described; "partly noise" is an argument, not a measurement. | — | as a stated limitation, not a measured one. |

## E. Stage 0 record and Stage 1 draft — assertions originating in this session

| # | exact assertion (file:line) | source | what the source establishes | assumptions | permitted wording |
|---|---|---|---|---|---|
| E1 | "the public code writes `looks = 1` as a literal" (S0 0.4; S1 B11) | `vol_model.py:176`, `har.py:110`: `"looks": 0 if label == "train" else 1` | Literal. | — | as stated. |
| E2 | "GARCH α = 0.2000, β = 0.7800 lie on arch 7.2.0's starting grid; fit status unknown" (S0 0.5; S1 B5) | VM `garch`; `arch.univariate.volatility.GARCH.starting_values` source: `alphas [.01,.05,.1,.2]`, `abg [.5,.7,.9,.98]` | Parameters equal a grid point; convergence not recorded. The audit's "the optimiser did not move" is a reviewer inference, not verified. | — | as stated; "did not move": unknown. |
| E3 | "sealed_2022: 87,552 rows = 304 days; sealed_recent: 87,264 = 303 days; the last two months of each block are absent under the min-training-rows rule" (S0 0.5; S1 B6) | VM `n`; 288 bars/day; `vol_model.py:78-87` with TRAIN_MONTHS 12, PURGE_DAYS 3, MIN_TRAIN_ROWS 20,000: for Nov 2022 the training window ∩ training mask is ≈ 58 days ≈ 16.7k rows < 20k; Dec 2022 ≈ 8k; Jul/Aug 2026 similarly | 87,552/288 = 304 and 87,264/288 = 303 complete-day equivalents; actual distinct-day coverage requires the complete-day assumption or preserved timestamp membership. The skipped months follow from the rule by arithmetic; not read from a log. | 288 complete bars per evaluated day. | "87,552 and 87,264 evaluated rows per block (304 and 303 complete-day equivalents); by arithmetic from the minimum-training-rows rule, the last two months of each block have no forecasts." |
| E4 | "corr(err 1h, err 4h) = 0.574 row-level, 0.823 at daily-MSE level … error autocorrelation 0.92/0.98 at lag 1, ≈ 0 beyond one horizon" (S1 B6) | preserved diagnostic `audit/diagnostics/d3_error_dependence.py` (+ `.out`, run 2026-09-12): 366,641 training rows from `model_a.parquet` × `grid.parquet`; R² reproduced (0.660 / 0.678 = VM train) | as stated for training rows and the log-vol error; the preserved output records the figures. | Training-row dependence resembles sealed-row dependence. | as stated, "training rows; see diagnostics/d3". |
| E5 | "mean Σ\|Δw\| = 1.888 per week in units of one leg's notional (94 % of gross), ≈ 20.8 entries, 20.7 exits, 2.5 flips" (S0 0.5; S1 F11) | preserved diagnostic `audit/diagnostics/d1_c18_traded_notional.py` (+ `.out`) (A9) | as stated under equal weights rebalanced weekly with drift ignored. | Target-weight changes ignoring drift are not realised rebalance trades. | as stated with the drift assumption; "see diagnostics/d1". |
| E6 | "24 of the 72 OI-covered print days fall inside the two sealed blocks" (S1 v1, audit-forwarded) | preserved diagnostic `audit/diagnostics/d2_print_days.py` (+ `.out`): 73 OI-covered first-of-month print days; 26 inside the sealed blocks including the ±3-day purge; 47 training | The forwarded figures were 72/24; the recomputed figures are 73/26 (purge included). | Purge included in "sealed". | "26 of 73, sealed blocks including the ±3-day purge (see diagnostics/d2)". The 72/24 figures: withdraw. |
| E7 | "61 includes next-day slivers of 47 real days" (S0 0.5; S1 B4) | preserved diagnostic `d2_print_days.out`: 47 first-of-month training print days; ES n_days 61; mechanism (spill via `grid.py` one-bar tolerance) not verified | 47 vs 61 is measured; the cause of the 14 extra day-blocks is inferred. | Spill mechanism. | "61 day-blocks against 47 first-of-month training days (see diagnostics/d2); the origin of the additional 14 blocks was not verified." |
| E8 | "prior SE of 0.264 %/wk" (S0 0.5; S1 F14) | `CANDIDATES.md:159` (0.74 unadjusted MDE, 1.0 at N = 20, σ_w ≈ 5 %, 360 weeks) | 0.74/2.80 = 0.264; 1.0/3.86 = 0.259; 5 %/√360 = 0.264. | That 0.74 was computed as 2.80 × SE (the line labels it MDE). | as stated. |
| E9 | "the real difference uses min cell 15; placebo draws use min cell 3" (S1 B2 / P-8) | `audit.py:128,136,170` | as stated. | — | as stated. |
| E10 | "30-day calendar legs entered weekly overlap ~4×" (S1 B8) | `instrument.py:261-291`: one entry per `entry_day` (weekly), 30-day short leg | Weekly entries of a 30-day leg overlap by construction. | — | as stated. |
| E11 | "C15 was run one minute after C18 was recorded as cleared" (S0 0.3) | private `LOG.md` 17:41 / 17:42 | as stated. | — | as stated. |
| E12 | "`prereg.py` computes every design SE from the same data the test then uses" (S0 0.3) | `prereg.py:78-83`: `se = C.alpha_se(df)` on the actual C18 series; analogous for others | as stated for C18; the other designs' lines follow the same pattern (`rec(..., se, ...)` from the data). | — | as stated. |
| E13 | "the s0 stitch offsets, s1 schedule and s6 placebo flip rates read the full series including the sealed period" (S0 0.4; S1 R6) | `s0_stitch.py:88-95` (offsets scanned over the full series); `s1.py:58-60` per-market notional and dollar-vol medians over `tail(250)` (the final 250 observations, i.e. through 2026-09-09) and `:73,79` schedules and turnover evaluated from `config.EVAL_START` (2002-01-01) with no training-end cutoff; `s6.py:24-37` `load_scores` reads whole score parquets and `random_like` computes flips over them (whether the parquets contain sealed rows: not verified) | s0: offsets selected on full history including the sealed period. s1: cost normalisation uses the final 250 observations (inside the sealed period) and schedule turnover is evaluated from 2002 through the end of the series without a training cutoff. s6: whole score files are read; sealed-row content unknown. | — | s0 and s1 as stated; s6: "reads full-length score files; whether they include sealed rows was not established". |
| E14 | "the C18 placebo's expected funding leg is zero by symmetry" (S1 F11) | `run.py:203-217`: symbols drawn uniformly at random within each week for both legs | Under uniform random assignment the long and short legs have the same expected funding, so the expected difference is zero. | Uniform draws without replacement from the same pool (as coded). | as stated. |
| E15 | "the Calmar surface is flat at 0.02 with 0.03 at the top of the grid" (S1 F16) | `s8.md:51` | Calmar is 0.02 at every tested target from 5 % to 30 % and 0.03 at 40 %, to the report's two-decimal precision; exact flatness is not established. | — | "flat to two decimals across 5–30 %, 0.03 at 40 %". |
| E16 | "initial-capital drawdown omitted" (S1, from the plan) | `backtest.py:84-86`; `simulator.py:22-25` `eq = cumprod(1+win)`, `peak = maximum.accumulate(eq)` | The running peak starts at the first day's equity, not at 1.0; a loss on day one is not a drawdown from initial capital. | — | as stated. |
| E17 | "The option-fill file was not produced by the public `fetch.py` as written" (S0 0.5) | `fetch.py:408-410` entry_days = Fridays from 2021-01-01, 00:00–04:00 UTC; preserved diagnostic `audit/diagnostics/d4_option_fills.py` (+ `.out`): 417 entry days from 2020-01-01, every weekday present, 87 % Fridays, hours 00–03 UTC | as stated. | — | as stated; "see diagnostics/d4". |
| E18 | "The private v1 variant mining did not touch sealed data: not demonstrable" (S0 0.3) | not audited | unknown. | — | unknown. |
| E19 | "Framing 3's momentum control was added after the raw result" (S1 B3) | `event_study.py:19-23` docstring: rationale for the control ("without that control the framing produces a beautifully monotone effect that is not about age at all"); private v3 registration (VERSIONS.md v3 sub-hypothesis (a)): a Spearman criterion on age quintiles with no momentum control | The control is absent from the v3 registration and present in the completed analysis; the docstring supplies the rationale, not the chronology. "Added after seeing the raw result" is the author's retrospective account as read from the docstring, not a dated record. | — | "The momentum control is not in the v3 registration and is present in the completed analysis; the docstring gives its rationale." "Added after seeing the raw result": permitted only as attributed to the author's retrospective account. |
| E20 | Stage 1 P-15: "Binance USDT-M taker at VIP0 5 bp" | external; not in any artefact | unknown here. | — | unknown; label external. |

## G. Assertions added after the verification pass

| # | exact assertion (file:line) | source | what the source establishes | assumptions | permitted wording |
|---|---|---|---|---|---|
| G1 | "Model A. Ridge on map-derived and market features, walk-forward monthly on a trailing year" (bR:49); "walk-forward" (bW) | `vol_model.py:74-87` (monthly slices, 12-month trailing training window, 3-day purge); `vol_model.py:47-50,144` GARCH fitted once on all non-sealed training rows (`train_ok`) and its forecast enters every slice as `log_garch` (`:146`); `liquidation_map.py:194-205` leverage mix calibrated once on all training print days and its features enter every slice | The ridge fit is walk-forward monthly. Two inputs are not: the GARCH parameters and the leverage mix are each fitted once on training rows spanning 2020-09 to 2025-08 and supplied to every slice, including the 2022 sealed block, whose training window therefore contains inputs fitted on later dates. | — | "The ridge model is refitted monthly on a trailing year; its GARCH and map-feature inputs are fitted once on all training rows (2020-09 to 2025-08) and supplied to every month, including the 2022 sealed block." "Walk-forward" without that qualification: withdraw. |
| G2 | "at a one-hour horizon its shortest input is already a full day stale, which handicaps it rather than tests it" (bW:105; `har.py:40-43` comment "a benchmark whose shortest input is 288 bars stale") | `har.py:52-59` `har_features`: `ret2.rolling(n).sum()` ending at bar t inclusive for n = 288, 2016, 8640 | The day component is a trailing one-day window ending at the current bar. Its information is current to bar t; it is a one-day average, not a one-day-lagged value. | — | "Corsi's shortest component is a trailing one-day average ending at the current bar." "A full day stale" / "288 bars stale": withdraw. |
| G3 | "opened at real Deribit bid/ask" (private VERSIONS.md:351); public: premium "at ask (long) / bid (short)" (`instrument.py:229`); "Real measured relative half-spreads are charged" (private v8-prep) | `instrument.py:180-215` `pick_straddle`: for the nearest-ATM strike with both a call and a put, the "buy" price is the amount-weighted mean of fills flagged `direction == buy` over the four-hour entry window, "sell" likewise; half-spread = (buy − sell)/2 when both exist and buy > sell, otherwise imputed from the other leg's half-spread; a missing side is imputed as the all-fill VWAP ± that half-spread; strips (`:123-128`, per audit) pool fills across strikes of an expiry | "Bid" and "ask" are amount-weighted averages of executed buy-side and sell-side trade prices over a four-hour window, with missing sides imputed; they are not contemporaneous quotes at an entry instant. | That transaction-side VWAPs over four hours approximate executable quotes at entry. | "Option entry prices are amount-weighted averages of buy-flagged and sell-flagged fills over the four-hour entry window, with missing sides imputed from the other leg's half-spread." "Real bid/ask" and "measured spread" without that description: withdraw. |
| G4 | "A 60-day pass rate is set by volatility, not by Sharpe: this path, Sharpe 0.06, passes 22.3% of the time at 15% vol, against the specification's synthetic prior of 23% for a Sharpe of 0.8." (fW:53) | C28: one historical path (22.3 %) and one unrecovered synthetic figure (23 %) | One realised path at one Sharpe and one asserted prior at another Sharpe are compared; nothing varies Sharpe while holding volatility fixed. | — | withdraw. |
| G5 | "Every signal that failed on power here — cross-sectional momentum, value, skewness, carry — becomes testable at proper power there, using the same machinery" (fW:65) | no artefact: no signal-specific design, effect assumption, dependence model or coverage for an equity panel exists in either repository | A forward projection with no supporting computation. | — | withdraw. |
| G6 | "computed before any return was read" (fW:15; `s1.py:1-3` "Nothing here reads a return") | `s1.py:53-54` `ret = read_parquet(w0.parquet)`; `dvol = backtest.dollar_vol(ret)` — the return panel is read to compute dollar volatility for position sizing; no realised strategy P&L is evaluated in s1 | s1 reads the return panel (for volatility); it does not evaluate realised strategy profitability. | — | "The cost ratio was computed without evaluating any strategy's realised return; the return panel was read to compute dollar volatility." "Before any return was read": withdraw. |
| G7 | S1 §1.3 R2: "derived 90 % CI (b) from the archived 95 % half-widths" (STAGE1_SPEC_DRAFT.md:243) | archived percentile-bootstrap 95 % CIs (asymmetric); no 90 % interval is stored | A 90 % interval cannot be derived from the endpoints of a percentile-bootstrap 95 % interval without the bootstrap distribution; the table's 90 % figures assume a normal interval with SE = half-width/1.96. | Normality and symmetry of the bootstrap distribution. | The derived 90 % intervals: withdraw. Only the stored 95 % intervals may be reported. |
| G8 | S1 §1.3 R2: "the P-5/P-6 recomputation will widen them (fewer, larger blocks; fewer events)" (STAGE1_SPEC_DRAFT.md:243) | none; a prediction | Whether regrouping widens or narrows an interval is not established until computed. | — | withdraw. |
| G9 | S1 §1.3 R2 retiring outcome: "or the trade-unit sample's design resolution (2.80 × SE) exceeds δ, in which case the claim is 'not resolvable', not 'null'" (STAGE1_SPEC_DRAFT.md:243) | none | An MDE exceeding the margin describes the design's resolution; it is not an equivalence-test outcome and is not itself a rejection or a gate. | — | withdraw as a gate; permitted only as a description of resolution reported beside the interval. |
| G10 | S1 P-17: "Calibrate the leverage mix on print days in odd calendar months, evaluate on even months. Justification: preserves regime coverage in both halves" (STAGE1_SPEC_DRAFT.md:219) | none | Alternating months interleave the two sets in time; adjacent-month dependence is not addressed, so this does not establish temporal separation between calibration and evaluation. | — | withdraw the adequacy claim; the split may be stated as a proposal whose temporal separation is not established. |
| G11 | S1 P-12: "QLIKE … is the loss a variance-payoff user bears" (STAGE1_SPEC_DRAFT.md:209) | none | QLIKE is a loss function with known consistency properties for variance-forecast ranking; that it equals the loss borne by a holder of a variance payoff is not established (a variance payoff is linear in realised variance; QLIKE is not). | — | withdraw. Permitted: "QLIKE is a loss consistent for ranking variance forecasts under a noisy proxy." |
| G12 | S1 §1.3 R6: "hold-out −1.28, Lo SE 0.71 → t ≈ −1.8, 95 % ≈ [−2.7, +0.1]" (STAGE1_SPEC_DRAFT.md) | C10 | 0.71 is the consistency check's training-based calculation, not an SE of the hold-out estimate. | — | withdraw the t-statistic and interval (as C10). |

## I. Subsequent additions — explicit evidence classes

These rows distinguish measurements, assumption-dependent interpretations, operational facts and author decisions. A source citation is necessary for each; being documentary does not make a statement exempt from checking. Closing the listed gaps does not certify exhaustive coverage.

| # | class | assertion or permitted summary | source | limit / permitted interpretation |
|---|---|---|---|---|
| I1 | (c) operational description | `src/fetch.py` "Fetches klines, open interest, funding, premium index, DVOL, liquidation prints, book snapshots and Deribit option fills from public endpoints." (bR:34) | `btc-public/src/fetch.py:153` `futures_klines`, `:160` `premium_index`, `:167` `funding`, `:185` `metrics` (open interest), `:210` `dvol`, `:240` `liquidations`, `:286` `book_snapshots`, `:349` `deribit_option_fills`; endpoints at `:35-36`, `:219`, `:357` | Function inventory matches the sentence. Whether the archived option-fill file came from `:349` on the stated schedule is E17, not this row. |
| I2 | (c) operational description | `src/grid.py` "Builds the five-minute grid with as-of joins, tolerances and staleness columns." (bR:35) | `btc-public/src/grid.py:29-53` `_join`: `pd.merge_asof` (`:41`) with `tolerance=pd.Timedelta(tolerance)` (`:44`) and a `tsu_<name>` staleness column (`:49`); `:57` `build` | Describes the join mechanics only; what the as-of test establishes is B18. |
| I3 | (c) operational description | `src/audit.py` "Masks, purge, block bootstrap, label placebo, block permutation, MDE calculator." (bR:42) | `btc-public/src/audit.py:21` `sealed_mask` (purge via `purge_days`), `:37` `training_mask`, `:51` `block_bootstrap`, `:71` `design_power` (MDE), `:98` `block_shuffle`, `:118` `placebo_labels`, `:149` `randomisation`, `:170` `matched_difference` | Capability inventory; the validity of each procedure is adjudicated in B6, B7, B8. |
| I4 | (c) operational description | "`data/` is gitignored" (bR:27) | `btc-public/.gitignore:2` `data/`; `futures-public/.gitignore:2` `data/` | Both repositories. |
| I5 | (c) operational description | "Data sources are keyless: Yahoo Finance, TreasuryDirect, Binance, EIA. `config.END` fixes the last bar at 2026-09-09." (fR:27) | `futures-public/stages/s0_fetch.py:24` `yf.Ticker(...).history` and `candidates/fetch.py:153` (Yahoo); `candidates/fetch.py:37` (TreasuryDirect), `:60,101,122` (Binance `fapi`), `candidates/splice_check.py:38` (EIA); no key or token parameter in any call; `futures-public/config.py:19` `END = "2026-09-09"` | Keyless as coded. |
| I6 | (c) operational description | Runtime and credential scope in the futures README | Saved reproduction record; `/usr/bin/python3 --version` reports 3.9.6. Configured fetch calls cited in I5 use public endpoints without an API-key argument. | Permitted: the saved run used Python 3.9.6 and the configured fetch paths are keyless. "Python 3.9 or later" as a compatibility guarantee and "no credentials anywhere" as a universal assertion are not established. |
| I7 | (a) sample measurement | "Framing 2: … n 4,036 on 70 book days" (bW:74) | `btc-public/reports/results/event_study.json:45-46` `"n": 4036, "n_days": 70` (`framings.2_pressure.slope`) | Count fields as stored; checked by `tests/test_documents.py`. |
| I8 | (a) sample measurement | "Framing 3: … n 28,220 on 1,373 days" (bW:76) | `btc-public/reports/results/event_study.json:63-64` `"n": 28220, "n_days": 1373` (`framings.3_age.raw_slope`) | Count fields as stored; checked by `tests/test_documents.py`. |
| I9 | (a) recorded run observations | BTC reproduction: the log reaches `pipeline complete`; result JSONs were skipped; wrapper timeout marker and no captured exit code. | [REPRODUCTION.md](REPRODUCTION.md), [BTC log](reproduction/btc-run.log), [comparison](reproduction/comparison.json); cached clone of local revision `1433ea5`. | The archived option-fill count is 633,631 (d4); the fetch log reports 524,945. No claim of identical grid values, exit 0, exact runtime, or recomputed instrument/economics results. The five saved result JSONs remain retained artifacts. |
| I10 | (a) recorded run observations | The saved futures reproduction logged exit 0; training stages and candidate reports matched the saved baseline, while s9 retained historical reports. | [Futures log](reproduction/futures-run.log) and [file comparison](reproduction/comparison.json), against takeover baseline `f0bf041`. | One historical run, before the final guard changes. Retained hold-out files are not a fresh scoring. Later local guard tests do not establish that another network run will succeed. |
| I11 | (b) evidential summary; (c) project scope | Neither programme establishes a tradable strategy from these results; research construction and inference are the relevant material, with no record of live execution, inventory management or continuous quoting. | A9/C13: C18 net alpha with costs in the series is uncomputed; A11–A16: unresolved option economics; C5–C10: futures point estimates after modelled costs, without Sharpe intervals; H3: scope of the implementation. | "No strategy return is presented net of costs" is withdrawn: the futures programme does report returns and Sharpes after modelled costs. Unresolved economic evidence does not establish universal unprofitability or candidate unreadiness. |
| I12 | (c) author decision | I would make the cost assumptions and the effect worth investigating explicit before searching for signals. P8's initial cost screen illustrates that process, conditional on its assumed gross Sharpe and cost model. | C2/G6 describe P8's initial screen under an assumed Sharpe and cost model; B19 supplies the limited private v1 cost account. | A stated future process choice, not a measured benefit. No assertion about when the private v1 arithmetic was computed relative to mining is needed or established by this row. |
| I13 | (c) author decision | I would stop when a registered halt condition fired and label any subsequent work as a separate exploratory continuation. | A3 and the attributed first-person halt record in the futures write-up. | A stated future process choice. It does not repair the historical stopping-rule departures. |
| I14 | (c) portfolio organisation | The portfolio groups P7/P8 as research, P6 as competition, P4 as applied work and P1/P2/P3/P5 as foundations, retaining their identifiers. | The portfolio README and corresponding project descriptions. | P1–P6 are outside this ledger's statistical audit. Summaries do not turn simulated inputs into historical observations or validate withheld competition code. |
| I15 | (c) tested code behaviour | Reproduction validates required outputs and retained hold-out reports; missing/empty splice data produces no research verdict; optional BTC document checks depend on individual available source files. | Current `futures-public/build.py`, `stages/s9.py`, `candidates/splice_check.py` and focused regression tests; `btc-public/tests/test_documents.py` and its partial-cache tests. | Offline fixture tests of guards only; no new scoring, market effect, runtime guarantee or full fresh-network certification. |

## F. Historical repair suggestions (not ledger rows)

These were suggestions from the original audit, not a current work queue or a condition for reporting the limited retained measurements. They have not been executed by this final presentation review.

1. Rows with "unknown" that a bounded computation could settle: A7 (recompute both absolute-move specifications on the same sample and cells), B10/E2 (re-fit GARCH with convergence diagnostics), E7 (trace the 14 extra day-blocks), E13 (check whether score parquets contain sealed rows), C30 (compute final sector shares).
2. Rows where the permitted wording depends on a repair not yet made: A9/C13 (costs inside the weekly series), A11–A16 (dispersion convention, spread in cost), B12 (an interval for the HAR difference), C5/C6/C10 (Sharpe intervals with denominator uncertainty).
3. Rows whose withdrawal removes downstream text: A2/D4/C29 (breadth chain across fW:45,55,63 and T:41,49), A13/D2 (instrument-ceiling conclusion across bW, bS, bR, T), A22/A21 (ground-truth headline across four files).


## H. Final presentation additions — provenance added 2026-09-13

These rows document construction examples and navigation added after the original audit. They do not amend the conclusions or permitted interpretations in A–G. The individual example values are new extractions of existing inputs, not newly estimated study results; independent verification is recorded separately.

| # | exact assertion (file:section) | source | what the source establishes | assumptions | permitted wording |
|---|---|---|---|---|---|
| H1 | bW, "One touch, from map state to measurement": the 2020-09-01 06:00 UTC event and its displayed arithmetic | `audit/WORKED_EXAMPLES.json` P7; cached `btc-public/data/grid/grid.parquet` and `data/interim/leverage_mix.json`; `src/liquidation_map.py` `bucket_of` / `run_map`; `src/event_study.py` `collect`, `_outcomes`, locator aggregation; A20–A21 and B17 | Prefix replay with the stored mix selects the first covered cluster touch: bucket 988, upward, previous close 11759.39, high 11810.93, lower edge 11786.061874; no previous accepted touch; distance 0.2268134%; mass 995398.090690 / total 17341431.198283 = 5.7400%, above the 5% threshold. The four locator-bar totals are 300841.56839, 320055.80572, 0, 0; sum 620897.37411. Close 11805.78 to 11869.83 gives signed 60-minute log continuation +54.106446 bps. This is one cluster-touch row, not an unpredicted-print row. Grid labels are bar ends: the 06:00 event uses the raw candle opened at 05:55; its liquidation total covers 05:55:00–05:59:59 UTC. | Stored calibrated map, not directly observed positions; market-wide locator not conditioned on level/side or within-bar ordering. Single event illustrates construction and cannot establish an effect. | The displayed inputs, bucket/direction classification, arithmetic and contribution of this one row may be reported as a construction example only, with these limits. No new aggregate or mechanism conclusion. |
| H2 | fW, "Worked example: constructing one C18 week": 2020-02-24 to 2020-03-02, the ten-name table, previous targets and cost-proxy arithmetic | `audit/WORKED_EXAMPLES.json` P8, including all eligible scores, selected prices/funding and preceding targets; cached `futures-public/data/raw/cand/binance_funding.parquet` and `binance_klines_1d.parquet`; `candidates/common.py` `c18_weekly` / `alpha_se`; `candidates/run.py` C18; A8–A9 and C13 | The first eligible week with a preceding eligible book has 23 eligible symbols and five per leg. The selected names, funding sums, daily closes and signed returns are recorded in the example inputs. Long mean −13.451434%, short mean +12.714295%, sum −0.737139%, price −0.843941%, funding +0.106802%; BTC −7.691750%. Targets ±0.2, gross 2, sum absolute target changes 2.8 ignoring drift, union turnover 0.6 and application of the report's rate 0.120000%. Code sums available funding with its missing-value handling; no seven-complete-day requirement. IOST and QTUM have three and four available days respectively; absent-price days remain NaN and are skipped by the sum, so their scores cover shorter windows. Price labels are candle opening dates but values are closes. The report subtracts the mean proxy charge from gross alpha and labels the result "net alpha" (the withdrawn A9 figure); no net alpha with costs entered into the weekly series has been computed. | Survivor-conditioned universe; funding calculation not reconciled to actual payment notionals/timestamps; no execution records; drift ignored in target changes; the 0.120000% is an illustration of the existing proxy, not an observed fee or repaired net alpha. | The table and displayed arithmetic may be reported only as construction of this one gross `(ls, btc)` observation and an illustration of the existing cost proxy. No net-alpha or execution-profitability conclusion. |
| H3 | bR/fR "Start here" and bW/fW "Relevance to a research or trading role" | `btc-public/run.py` stage list and artifact skip branch, B22; `futures-public/reproduce.sh`, `build.py`, C32; the linked tracked outputs, `futures-public/reports/stage_reports/s2.md`, `s3.md`, `s5.md` and `backtest.py` `summary`; A7–A10, A20–A21, B4–B6, B12, C5–C10, A3 | The routes identify which tracked outputs to read and which steps depend on untracked data. BTC default skips existing result JSONs while attempting missing data stages; explicit stage overrides recompute dependent results. The earlier futures checkpoint handling has been superseded: current output validation and retained-s9 behaviour are described in C32 and I15; the saved run is I10. The three standalone signal reports provide point Sharpes and mean-return MDEs in Sharpe units, without Sharpe intervals. The relevance paragraphs identify the documented construction, benchmark and inference work, and are application framing, not measured employability or trading-skill claims. | Successful acquisition depends on sources and prerequisites; B22's option provenance and C32's build limitations remain. Relevance to a role is the author's stated positioning, not an empirical finding. | Navigation and conditional command behaviour as described. Relevance may name existing work and explicitly present it as research/inference practice; no claim of live trading, execution or inventory experience. Any reproduction claim must identify regenerated and retained outputs and its dated evidence. |
